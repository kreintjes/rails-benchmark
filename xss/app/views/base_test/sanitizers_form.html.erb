<h1><%= params[:controller].humanize %> - <%= params[:action].humanize %> - <%= params[:method].humanize %></h1>

<%= form_tag(base_test_sanitizers_perform_path(encode_method(params[:method]), params[:option])) do %>
  <%= text_area_tag "input" %>
  <div class="actions">
    <%= submit_tag "Submit" %>
  </div>
<% end %>