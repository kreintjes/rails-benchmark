<h1><%= params[:controller].humanize %> - <%= params[:action].humanize %> - <%= params[:method].humanize %></h1>

<%= form_for(@all_types_object, :url => delete_test_class_destroy_all_action_path(params[:method]), :method => "delete") do |f| %>
  <h2>Conditions</h2>
  <%= render 'all_types_objects/fields', :f => f, :for_conditions => true %>
  <div class="actions">
    <%= f.submit "Destroy All types objects" %>
  </div>
<% end %>