<h1><%= params[:controller].humanize %> - <%= params[:action].humanize %> - <%= params[:method].humanize %></h1>

<%= form_for(@all_types_object, :url => update_test_class_update_update_path(params[:method]), :method => "put") do |f| %>
  <div class="field">
    <%= label_tag(:id, "id of object to edit:") %><br />
    <%= select_tag(:id, options_for_select(AllTypesObject.order(:id).all.map(&:id)), :multiple => @multi) %>
  </div>
  <%= render 'all_types_objects/fields', :f => f %>
  <div class="actions">
    <%= f.submit "Update #{@name}" %>
  </div>
<% end %>

<%= link_to 'Root', root_path %>