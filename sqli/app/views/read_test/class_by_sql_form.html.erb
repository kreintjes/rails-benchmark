<h1><%= params[:controller].humanize %> - <%= params[:action].humanize %> - <%= params[:method].humanize %></h1>

<%= form_tag read_test_class_by_sql_perform_path(params[:method], :option => params[:option]) do %>
  <%= render 'conditions'%>
  <div class="actions">
    <%= submit_tag("Submit") %>
  </div>
<% end %>